'use client'

const nav = [['01','Start here','start'],['02','Mental model','model'],['03','Setup & providers','setup'],['04','Surfaces','surfaces'],['05','Tools','tools'],['06','MCP servers','mcp'],['07','Skills & memory','skills'],['08','Multi-agent work','agents'],['09','Automation','automation'],['10','Safety & operations','safety']]
const sources = [
  ['Official documentation','https://hermes-agent.nousresearch.com/docs/'],
  ['Documentation index (LLMs)','https://hermes-agent.nousresearch.com/docs/llms.txt'],
  ['Hermes Agent source','https://github.com/NousResearch/hermes-agent'],
  ['Nous Research','https://nousresearch.com/']
]
function Flow({items}:{items:[string,string][]}){return <div className="flow">{items.map(([a,b],i)=><span className="flow-wrap" key={a}><div className="step"><strong>{a}</strong><span>{b}</span></div>{i<items.length-1&&<div className="arrow">→</div>}</span>)}</div>}
function Code({children}:{children:React.ReactNode}){return <pre className="code"><code>{children}</code></pre>}
export default function Home(){return <div className="shell">
  <aside className="side"><a className="brand" href="#top"><span className="mark">✦</span><span>Hermes<br/>Field Guide</span></a><small>OPERATING MANUAL · 01</small><nav className="nav">{nav.map(([n,label,id])=><a href={'#'+id} key={id}><span className="num mono">{n}</span>{label}</a>)}</nav></aside>
  <main className="main" id="top"><header className="top"><span className="mono">HERMES / FIELD GUIDE</span><a href="https://github.com/NousResearch/hermes-agent"><button>View source ↗</button></a></header>
  <div className="wrap"><section className="hero"><div className="eyebrow">A practical map of the agent</div><h1>Make the machine<br/><em>useful.</em></h1><p>Hermes is an open-source, provider-agnostic agent that turns a model into an operator: it can reason, use tools, remember context, run on schedules, talk through messaging platforms, and delegate work.</p><div className="chips"><span className="chip">CLI + TUI</span><span className="chip">Desktop + dashboard</span><span className="chip">MCP-native</span><span className="chip">Multi-agent ready</span></div></section>
  <section className="section" id="start"><div className="label">01 / Start here</div><h2>From zero to first useful run</h2><p className="section-intro">The fastest path is intentionally small: install, choose a provider, ask one bounded question, then add capabilities as the work demands.</p><Flow items={[["Install","The shell installer sets up uv, Python, the environment, and launcher."],["Configure","Run the setup wizard and select a model/provider."],["Chat","Use the default interactive surface or one-shot mode."],["Extend","Add skills, MCPs, memory, schedules, or channels."]]}/><Code><span className="dim"># install</span>{'\n'}curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash{'\n\n'}<span className="dim"># setup + health check</span>{'\n'}hermes setup{'\n'}hermes doctor{'\n\n'}<span className="dim"># first conversation</span>{'\n'}hermes{'\n'}hermes chat -q "Summarize the files in this project"</Code></section>
  <section className="section" id="model"><div className="label">02 / Mental model</div><h2>One core, many surfaces</h2><p className="section-intro">Keep the mental model simple: a channel delivers intent, the orchestrator selects a model and tools, and the result is persisted, delivered, or delegated.</p><div className="card wide"><Flow items={[["Channels","CLI, Telegram, Discord, Slack, email, webhooks"],["Orchestrator","Context, approvals, model routing, tool loop"],["Capabilities","Built-in tools, MCP servers, skills, memory"],["Outcomes","Files, messages, schedules, code, reports"]]}/></div><div className="grid"><article className="card"><h3>Provider-agnostic</h3><p>Use OpenRouter, Anthropic, OpenAI, Google, DeepSeek, xAI, local models, and other supported providers. Swap providers without redesigning the workflow.</p></article><article className="card"><h3>Profile-safe</h3><p>Profiles isolate configuration, sessions, skills, memory, and runtime state. Resolve paths through <span className="mono">$HERMES_HOME</span> rather than hardcoding them.</p></article><article className="card"><h3>Persistent by design</h3><p>Sessions, memory, logs, and reusable skills make the system improve across conversations instead of resetting to zero.</p></article></div></section>
  <section className="section" id="setup"><div className="label">03 / Setup & providers</div><h2>Configure the boundary, not the chaos</h2><p className="section-intro">Settings describe behavior; secrets stay in the environment file. Use Hermes commands for configuration so live state remains valid.</p><div className="grid"><article className="card"><h3>Important paths</h3><ul><li><span className="mono">~/.hermes/config.yaml</span> — settings</li><li><span className="mono">~/.hermes/.env</span> — secrets only</li><li><span className="mono">~/.hermes/state.db</span> — canonical sessions</li><li><span className="mono">~/.hermes/skills/</span> — reusable procedures</li><li><span className="mono">~/.hermes/logs/</span> — diagnostics</li></ul></article><article className="card"><h3>Model workflow</h3><p>Start with the wizard, inspect available providers, then choose the model appropriate to cost, speed, context, and tool-calling reliability.</p><Code>hermes model{`\n`}hermes config get model</Code></article><article className="card"><h3>Configuration rule</h3><p>Do not hand-edit <span className="mono">config.yaml</span> for routine changes. Prefer explicit commands such as:</p><Code>hermes config set display.interface tui</Code></article></div></section>
  <section className="section" id="surfaces"><div className="label">04 / Surfaces</div><h2>Meet Hermes where work happens</h2><div className="grid"><article className="card"><h3>Terminal & TUI</h3><p>The default CLI is the shortest feedback loop. The Ink TUI adds a richer terminal interface and docked widgets for persistent workflows.</p></article><article className="card"><h3>Desktop app</h3><p>A native Electron surface with streaming chat, sessions, command palette, files, notifications, and remote-gateway profiles.</p></article><article className="card"><h3>Dashboard & proxy</h3><p>The dashboard exposes administration and embedded chat. The OpenAI-compatible proxy lets other clients use a local Hermes-backed endpoint.</p></article><article className="card"><h3>Messaging</h3><p>Connect Telegram, Discord, Slack, WhatsApp, iMessage, Signal, Matrix, Teams, email, and more while retaining tool access.</p></article><article className="card"><h3>Webhooks</h3><p>Turn external events into agent runs. Make inputs explicit, validate payloads, and protect routes with authentication and approval boundaries.</p></article><article className="card"><h3>IDE workflows</h3><p>Use the ACP server to connect agent capabilities to supported IDE experiences without creating a second knowledge system.</p></article></div></section>
  <section className="section" id="tools"><div className="label">05 / Tools</div><h2>Tools are the hands</h2><p className="section-intro">Hermes can inspect and change the world through a deliberate tool layer: files, shell, browser, code execution, communication, scheduling, delegation, and external services.</p><div className="grid"><article className="card"><h3>Local execution</h3><p>Read and write files, run commands, inspect processes, build software, test artifacts, and verify system state.</p></article><article className="card"><h3>Web & research</h3><p>Search, extract, inspect live pages, use browser automation, and ground conclusions in source material.</p></article><article className="card"><h3>Communication</h3><p>Send and triage email, route Telegram teams, post to supported social channels, and deliver scheduled reports.</p></article><article className="card"><h3>Orchestration</h3><p>Delegate subtasks, spawn independent agents, schedule cron jobs, chain outputs, and preserve continuity.</p></article><article className="card"><h3>Knowledge</h3><p>Use persistent memory, Markdown vaults, QMD search, and source-backed notes as separate but composable layers.</p></article><article className="card"><h3>Safety</h3><p>Keep approval modes, redaction, credential boundaries, and external side effects visible before execution.</p></article></div></section>
  <section className="section" id="mcp"><div className="label">06 / MCP servers</div><h2>Plug in specialized capability</h2><p className="section-intro">MCP gives Hermes a standard boundary for external tools and resources. Add a server when a capability deserves its own integration surface, lifecycle, or permission boundary.</p><Flow items={[["Discover","Inspect available MCPs and their tools."],["Register","Use the Hermes MCP commands and explicit server config."],["Test","Verify connection, tool discovery, and a safe read operation."],["Operate","Document scope, credentials, failures, and rollback."]]}/><Code>hermes mcp add &lt;name&gt; --transport stdio -- npx -y &lt;server-package&gt;{`\n`}hermes mcp test &lt;name&gt;{`\n`}hermes mcp list</Code><div className="grid"><article className="card"><h3>Good MCP candidates</h3><p>Databases, issue trackers, design systems, cloud APIs, knowledge bases, observability systems, and local specialist servers.</p></article><article className="card"><h3>Local vs hosted</h3><p>Prefer local/self-hosted MCP servers for sensitive data and predictable control. Use hosted services when trust, network, and data boundaries are understood.</p></article><article className="card"><h3>Design rule</h3><p>One server should expose a coherent capability. Keep tool names readable, permissions narrow, and read/write effects distinguishable.</p></article></div></section>
  <section className="section" id="skills"><div className="label">07 / Skills & memory</div><h2>Turn experience into leverage</h2><div className="grid"><article className="card"><h3>Skills</h3><p>A skill is a reusable operating procedure: when to use it, exact steps, pitfalls, verification, and references. Load the matching skill before specialized work.</p><Code>hermes skills list{`\n`}hermes skills view &lt;name&gt;</Code></article><article className="card"><h3>Memory</h3><p>Save stable user preferences, environment facts, and reusable conventions. Do not store secrets, temporary progress, or stale task logs.</p></article><article className="card"><h3>Knowledge bases</h3><p>Keep Markdown as source of truth, use QMD as an index, and separate company knowledge from Hermes operational memory.</p></article></div></section>
  <section className="section" id="agents"><div className="label">08 / Multi-agent work</div><h2>Parallelism with a purpose</h2><p className="section-intro">Use the smallest unit that provides the required isolation. Delegate bounded subtasks for speed; spawn full Hermes processes for long-running, interactive, or fully independent missions.</p><div className="grid"><article className="card"><h3>Delegate</h3><p>Short-lived, isolated context. Give a precise goal, background, constraints, and output shape. Best for research, review, and independent slices.</p></article><article className="card"><h3>Spawn</h3><p>Independent subprocess with its own sessions, tools, and environment. Use <span className="mono">hermes chat -q</span> for one-shot work or tmux for interactive PTY sessions.</p></article><article className="card"><h3>Coordinate</h3><p>Split by responsibility, define shared artifacts, relay interfaces explicitly, and aggregate results. Use worktrees when multiple agents edit code.</p></article></div><Code><span className="dim"># long-running independent mission</span>{'\n'}hermes chat -q "Research the market and write a cited brief to ./research.md"{'\n\n'}<span className="dim"># resume a previous session</span>{'\n'}hermes --continue{'\n\n'}<span className="dim"># schedule recurring work</span>{'\n'}hermes cron add "0 9 * * 1" "Prepare the weekly engineering digest"</Code></section>
  <section className="section" id="automation"><div className="label">09 / Automation</div><h2>Make repeatable work durable</h2><div className="card wide"><Flow items={[["Trigger","Schedule, webhook, message, or manual run"],["Procedure","Script for deterministic work; skill for judgment"],["Execution","Cron, delegated agent, Bot/profile, or process"],["Output","File, message, external record, or report"]]}/></div><p className="section-intro">Automation is not just recurrence. Define timezone, inputs, output destination, deduplication, continuity, retry behavior, and what happens when a side effect needs approval.</p></section>
  <section className="section" id="safety"><div className="label">10 / Safety & operations</div><h2>Keep power legible</h2><div className="grid"><article className="card"><h3>Protect secrets</h3><p>Credentials belong in <span className="mono">.env</span> or managed auth stores—not Markdown, prompts, logs, screenshots, or config committed to Git.</p></article><article className="card"><h3>Approve effects</h3><p>Separate read operations from writes. Confirm external messages, destructive changes, production schedules, and payments before execution.</p></article><article className="card"><h3>Verify outcomes</h3><p>Read back the exact target after API mutations, test deployed URLs, inspect generated files, and report honest limitations.</p></article></div><Code>hermes doctor{`\n`}hermes config get display.interface{`\n`}hermes mcp test &lt;name&gt;</Code></section>
  <section className="section"><div className="label">Sources</div><h2>Go deeper when the work demands it</h2><p className="section-intro">This guide is a compressed orientation, not a replacement for the live documentation. Start here, then follow the official page for the exact feature or command.</p><div className="grid">{sources.map(([name,url])=><a className="card source" href={url} key={url}><h3>{name} ↗</h3><span>{url.replace('https://','')}</span></a>)}</div></section></div><footer><span className="mono">HERMES FIELD GUIDE</span> · summarized for practical operation · built with Next.js + Tailwind CSS</footer></main></div>}
